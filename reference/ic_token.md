# Convert raw data into a tibble of xcmsRaw objects.

Convert raw data into a tibble of xcmsRaw objects.

## Usage

``` r
ic_token(user, pass)
```

## Arguments

- user:

  ICMeter username

- pass:

  ICMeter password

## Value

A character vector containing the ICMeter token
