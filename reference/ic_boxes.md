# Convert raw data into a tibble of xcmsRaw objects.

Convert raw data into a tibble of xcmsRaw objects.

## Usage

``` r
ic_boxes(token)
```

## Arguments

- token:

  A character vector containing the ICMeter token

## Value

data.frame containing information about each box registered on the token
account
